import Home from "@/app/(component)/Home/Home";
import { render } from "@testing-library/react";

describe("Home", () => {
    it("should render the login page if the user is not logged in, otherwise render the dashboard", () => {

    });
});
